PORT=3000
JWT_SECRET=your_cryptographically_secure_random_jwt_secret_here

# SMTP Configuration for Email Notifications (e.g. Resend, Gmail SMTP, Mailtrap, etc.)
# If left blank, the application will automatically spin up a temporary test inbox using Ethereal Email.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

